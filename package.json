{
  "name": "qr-shadow-stl",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "tsc -p tsconfig.test.json && \"%npm_node_execpath%\" --test .test-dist/tests",
    "test:debug": "tsc -p tsconfig.test.json && \"%npm_node_execpath%\" --test .test-dist/tests"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "vite": "^7.2.4"
  }
}
